
# ===================
# Django
# ===================
DEBUG=True
SECRET_KEY=
ALLOWED_HOSTS=localhost,127.0.0.1

# ===================
# Base de données
# ===================
DB_NAME=seeg
DB_USER=seeg
DB_PASSWORD=changeme
DB_HOST=localhost
DB_PORT=5432

# ===================
# CORS (origines autorisées à appeler l'API depuis un navigateur)
# ===================
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ===================
# Cache / Redis (nécessaire pour que l'OTP fonctionne correctement
# dès que plusieurs workers Gunicorn tournent en prod)
# ===================
REDIS_URL=

# ===================
# SMS (OTP agents terrain)
# ===================

SMS_PROVIDER_URL=
SMS_PROVIDER_API_KEY=
SMS_SENDER_ID=SEEG

# ===================
# Email (SMTP) — optionnel, pas encore utilisé par le code actuellement
# ===================
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=noreply@seeg.ga
